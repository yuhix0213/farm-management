-- ============================================================
--  牧場管理システム  DB初期化 SQL
--  ファイル 00: データベース作成
--
--  【実行方法】
--  phpMyAdmin にログインして「SQL」タブを開き、
--  以下の1行だけを貼り付けて実行してください。
--
--  ※ COMMENT句を削除（MySQL 5.7以前は非対応のため）
--  ※ rootまたはDB作成権限のあるユーザーで実行すること
-- ============================================================

CREATE DATABASE IF NOT EXISTS farm_management
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- ============================================================
--  ▼ #1044エラー（アクセス拒否）が出た場合
-- ============================================================
--  現在ログイン中のユーザー（skyhash77等）にDB作成権限がありません。
--  以下のいずれかで対処してください：
--
--  【方法A】phpMyAdminの左サイドバー上部「新規作成」から
--           GUIでデータベースを作成する（推奨）
--           → データベース名: farm_management
--           → 照合順序: utf8mb4_unicode_ci
--
--  【方法B】rootユーザーでphpMyAdminにログインし直して実行する
--
--  【方法C】ホスティング会社のコントロールパネルから
--           DBを新規作成する（さくら・ロリポップ等）
-- ============================================================

SELECT 'farm_management データベース作成完了' AS result;
